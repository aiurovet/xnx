name: doul
description: Command-line utility to expand text content by placeholders and to run external utilities.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.11.0 <3.0.0'

dependencies:
  archive: ^3.0.0
  args: ^2.0.0
  json_path: ^0.3.0
  file: ^6.0.0
  glob: ^2.0.0
  path: ^1.6.4
  process_run: ^0.12.0

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
